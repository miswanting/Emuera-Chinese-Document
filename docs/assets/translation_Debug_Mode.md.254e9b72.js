import{o as e,c as d,a as o}from"./app.e360dedc.js";const a='{"title":"调试模式","description":"","frontmatter":{},"headers":[{"level":2,"title":"在调试模式下启动Emuera","slug":"在调试模式下启动emuera"},{"level":2,"title":"在调试模式下的操作","slug":"在调试模式下的操作"},{"level":2,"title":"调试窗口","slug":"调试窗口"}],"relativePath":"translation/Debug_Mode.md","lastUpdated":1631248272178}',c={},p=[o('<h1 id="调试模式" tabindex="-1">调试模式 <a class="header-anchor" href="#调试模式" aria-hidden="true">#</a></h1><p>调试模式是1.750版中增加的模式。</p><p>这个功能与 &quot;使用调试命令 &quot;选项无关。</p><h2 id="在调试模式下启动emuera" tabindex="-1">在调试模式下启动Emuera <a class="header-anchor" href="#在调试模式下启动emuera" aria-hidden="true">#</a></h2><p>要在调试模式下启动Emuera，请将<code>-Debug</code>作为命令行参数。</p><p>传递命令行参数的一个简单方法是：</p><p>右键点击EmueraXXXX.exe，选择 &quot;创建快捷方式&quot;。</p><p>在创建的快捷方式上点击右键，选择 &quot;属性&quot;。</p><p>在属性中，在 &quot;链接到&quot;字符串的末尾添加<code>-Debug</code>。</p><p>（如果链接是用&quot;&quot;括起来的，比如 &quot;C:~~\\EmueraXXX.exe&quot;，在<code>&quot;&quot;</code>后面加上<code>-Debug</code>）</p><p>双击该快捷方式，在调试模式下启动它。</p><h2 id="在调试模式下的操作" tabindex="-1">在调试模式下的操作 <a class="header-anchor" href="#在调试模式下的操作" aria-hidden="true">#</a></h2><p>当你在调试模式下启动时，会自动创建一个名为debug的文件夹。</p><p>变量观察列表和调试控制台日志，如下所述，都保存在调试文件夹中。</p><p>当处于调试模式时，除了正常执行的行之外，以<code>;#;</code>开头的行和<code>[IF_DEBUG]</code>和<code>[ENDIF]</code>之间的行也会被执行。</p><p>当不在调试模式时，这些行被当作注释，不被执行。</p><p>相反，当处于调试模式时，<code>[IF_NDEBUG]</code>和<code>[ENDIF]</code>之间的行被视为注释，不被执行。</p><div class="language-"><pre><code>;#;PRINTL 这一行只在调试模式下执行。\n[IF_DEBUG]\n  PRINTL 这一行只在调试模式下执行。\n[ENDIF]\n[IF_NDEBUG]\n  PRINTL 这一行只有在非调试模式下才会执行。\n[ENDIF]\n</code></pre></div><p>此外，还有一些指令只在调试模式下发挥作用。</p><p><code>DebugPrint</code>、<code>DebugPrintForm</code>、<code>DebugPrintL</code>和<code>DebugPrintFormL</code>指令的功能与<code>Print</code>语句相同，只是输出到调试窗口。</p><p>如果参数为零，<code>Assert</code>指令就会产生错误。 如果参数为非零，它就不做任何事情。</p><p><code>DebugClear</code>指令清除调试窗口的所有字符。没有参数。</p><p>这些指令在非调试模式下没有任何作用。</p><p>它也不解析参数，所以如果有问题，比如一个错误的<code>DebugPrintForm</code>，在非调试模式下什么也不会发生。</p><p>它们被视为无用的指令，而不是注释行，所以它们在<code>SIF</code>语句之后立即使用是安全的。</p><p>此外，变量<code>__FILE__</code>，<code>__LINE__</code>和<code>__FUNCTION__</code>只在调试模式下返回有意义的值。</p><p>在非调试模式下，它们返回0或一个空字符串。</p><h2 id="调试窗口" tabindex="-1">调试窗口 <a class="header-anchor" href="#调试窗口" aria-hidden="true">#</a></h2>',28)];c.render=function(o,a,c,r,t,u){return e(),d("div",null,p)};export{a as __pageData,c as default};
